package me.mastrio.adventuresawakening;

import net.fabricmc.api.ModInitializer;
import org.apache.logging.log4j.LogManager;
import org.apache.logging.log4j.Logger;

public class AdventuresAwakening implements ModInitializer {

    // Logger to log things to the console that contains thing that were logged to it using loggers such as this logger that is right here and is used for logging things into the logger
    public static final Logger LOGGER = LogManager.getLogger("modid");

    // Initialize the mod
    @Override
    public void onInitialize() {

    }

}
